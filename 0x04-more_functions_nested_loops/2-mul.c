#include "main.h"

/**
  * mul - multiplies 2 digits
  * @a: first digit
  * @b: second digit
  * Return: product of the 2 digits
  */

int mul(int a, int b)
{
	int a, b, res;

	res = (a * b);
	return (res);
}
