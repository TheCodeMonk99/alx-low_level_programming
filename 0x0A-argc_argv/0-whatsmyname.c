#include <stdio.h>
#include "main.h"

/**
  * main - entry point
  * @argc: argument count
  * @argv: argument vector
  * Return: Always 0 success
  */
int main(int argc __attribute__((unused)), int argv)
{
	int i = 0;

	printf("%s\n", argv[0]);
	return (0);
}
