#include "main.h"

/**
  * puts2 - prints every character in a string
  * @str - char to be printed
  *
  * Return - character
  */

void puts2(char *str)
{
	int i;

	for (i = 0; str [i] != '\0'; i++)
	{
		_putchar(s[i]);
		_putchar(10);
	}
}
