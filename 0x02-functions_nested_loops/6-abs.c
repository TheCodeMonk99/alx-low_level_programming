#include "main.h"

/**
  * _abs - computes absolute value of int
  * @n: num to be checked
  * Return: the absolute value of digit
  */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
